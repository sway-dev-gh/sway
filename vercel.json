{
  "buildCommand": "npm install && cd web && npm install && npm run build",
  "installCommand": "echo 'Using custom buildCommand'",
  "outputDirectory": "web/.next",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://api.swayfiles.com/api/:path*"
    }
  ]
}
