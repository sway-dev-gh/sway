{
  "buildCommand": "cd web && npm ci && npm run build",
  "outputDirectory": "web/.next",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://api.swayfiles.com/api/:path*"
    }
  ]
}
